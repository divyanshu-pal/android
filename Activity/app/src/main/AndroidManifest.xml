<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Activity"
        tools:targetApi="31">

        <!-- Make MainScreenActivity the launcher activity -->
        <activity
            android:name=".MainScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare other activities -->
        <activity android:name=".Screen1Activity" android:exported="true" />
        <activity android:name=".Screen2Activity" android:exported="true" />
        <activity android:name=".Screen3Activity" android:exported="true" />
        <activity android:name=".Screen4Activity" android:exported="true" />
        <activity android:name=".Screen5Activity" android:exported="true" />
    </application>

</manifest>
